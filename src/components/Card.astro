---
interface Props {
	title: string;
	image: string;
}

const { title, image } = Astro.props;
---

<li class=" w-32 flex items-center justify-center">
		<img src={image} alt={title} class=" w-3/4" />
</li>
